# this is list helper module nd it will be use for future import try.py

def find_max(numbers):
    return max(numbers)

def find_min(numbers):
    return min(numbers)

def find_average(numbers):
    return sum(numbers)/len(numbers)